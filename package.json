{
  "name": "hadrix-flow",
  "version": "0.1.0",
  "private": true,
  "description": "Deterministic JS/TS flow-fact generator (infrastructure, not a vulnerability scanner).",
  "license": "UNLICENSED",
  "type": "module",
  "bin": {
    "hadrix-flow": "src/cli.ts"
  },
  "scripts": {
    "dev": "node src/cli.ts",
    "build": "node scripts/build.ts",
    "test": "node --test test/*.test.js",
    "typecheck": "node --check src/cli.ts scripts/build.ts"
  },
  "dependencies": {
    "typescript": "5.9.3"
  }
}
